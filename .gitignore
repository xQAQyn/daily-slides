# --- 1. Node.js 依赖 (必须忽略) ---
# node_modules 包含数万个小文件，绝对不要提交到 Git
node_modules/
# npm 报错日志
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# --- 2. 操作系统生成的文件 (垃圾文件) ---
# Mac
.DS_Store
.AppleDouble
.LSOverride
._*

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# Linux
*~
.fuse_hidden*
.directory
.Trash-*

# --- 3. 编辑器/IDE 配置 (个人配置不提交) ---
# VS Code
.vscode/*
# !.vscode/extensions.json # 如果你想共享推荐插件列表，可以取消这行的注释
!.vscode/settings.json   # 如果你想强制统一格式化配置，可以取消这行的注释

# JetBrains (WebStorm/IntelliJ)
.idea/
*.iml

# Vim / Emacs 临时文件
*.swp
*.swo
\#*#

# --- 4. 项目特定配置 ---
# 这里的策略是：
# node_modules 被忽略，但 vendor (也就是 copy-lib.js 生成的文件夹) 被保留。
# 这样 GitHub Pages 可以直接读取 vendor 里的 reveal.js 核心文件。

# 如果你未来决定使用 GitHub Actions 在云端自动构建，
# 那么可以取消下面这行的注释，把 vendor 也忽略掉：
vendor/

# 临时测试或草稿目录 (可选)
temp/
drafts/